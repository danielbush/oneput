---
id: MENU_LIKE__WORK
status: active
created: 2026-07-22
package: oneput
summary: Extract a MenuLike contract so calendar (2D day focus) and chat keep menu-area behaviours without abusing MenuItem/ignored or replaceMenuUI.
---

# feat: MenuLike contract

Generalise what bindings / AppController expect from the menu area (move focus,
activate, optional filter/generative, identity, back) into a **MenuLike**
contract. Keep `MenuItem` as the list MenuLike’s model; do not route
calendar/chat through `replaceMenuUI`.

**Vocabulary / guide:** [`packages/oneput/docs/CONCEPTS.md`](../../packages/oneput/docs/CONCEPTS.md) (MenuLike section)

Working name is MenuLike — rename later if needed.

## Changes

- **2026-07-22** — decided: generalise menu-area contract, not MenuItem; calendar = day focus (not week-as-item); replaceMenuUI stays outside; dropped “surface” for **MenuLike**; concepts live in `docs/CONCEPTS.md`
- **2026-07-22** — dropped `ctl.menuLike`; catalog uses `ctl.menu` (`MenuController implements MenuLike`). Holder only if demos require it.
- **2026-07-22** — `moveFocus` takes directions (not list/grid deltas). Sketched `CalendarMenu` + `ChatMenu` under `shared/ui/menu/` to surface contract gaps (`getItems`, install via `setMenu`, month chrome, scroll anchor).
- **2026-07-22** — implementation parked on branch `menu-like`; `main` continues with rich-menu-item spike. Revisit if that spike fails.

## Tasks

- [x] Capture MenuLike idea in `docs/CONCEPTS.md`
- [x] Point from `docs/architecture.md` and `AGENTS.md`
- [x] Extract `MenuLike` contract; list (`MenuController` / `ctl.menu`) is sole implementation; catalog uses `moveFocus`/`activate`
- [ ] Calendar / Eliza demos steer what else belongs on MenuLike (no separate `ctl.menuLike` accessor)
- [ ] Holder / swap of non-list MenuLike only if demos prove `ctl.menu` can’t host it
- [ ] Calendar day-grid MenuLike (retire week-as-item mockup)
- [ ] Chat: transcript out of actionable list; suggestions stay list items
- [ ] Clarify `ignored` → chrome role (rename when cheap)
- [ ] Align events + oneput skill when public API settles

## Housekeeping

- Update mockup copy in `src/routes/+page.svelte` / `_mockups.ts` when calendar MenuLike lands
- Consider backlog chat/calendar mockup bullets in `work/backlog/oneput.md` absorbed by this ticket
- **Alternative path (also active):** [RICH_MENU_ITEM_WIDGETS__WORK](./20260722.spike.rich-menu-item-widgets.md) — calendar/chat as rich items + item-scoped bindings (spike before more MenuController surgery)
