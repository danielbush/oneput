---
id: REMOVE_TOKEN_MANAGER__WORK
status: done
package: jsed
created: 2026-03-27
summary: Remove TokenManager — its role has been absorbed by TokenCursor and EditManager
outcome: TokenManager deleted. Its only method (tokenize) moved to lib/token.ts as `quickDescend`. TokenCursorBase no longer takes a tokenManager param. EditManager creates TokenCursor directly. Test helpers simplified — no TokenManager wiring. Test file renamed to quickDescend.test.ts. Architecture docs updated.
---

# refactor: remove TokenManager

## Initial Proposal

TokenManager's role has been largely absorbed by TokenCursor's lazy tokenization (via `onEnterBlockTransparent` callback in `moveNext`/`movePrevious`). Review whether TokenManager still serves a purpose; if not, remove it and let the CURSOR own tokenization directly.

Once removed, simplify `createCursor` and `tokenizeAndCursor` test helpers in `TokenCursor.test.ts` — they currently wire up TokenManager as an intermediary.

## Tasks

- [x] Audit what TokenManager still does
- [x] Move remaining logic into TokenCursor or EditManager
- [x] Remove TokenManager
- [x] Simplify test helpers

## Changes

- **2026-03-28** — TokenManager had zero state; its `tokenize` method moved to `lib/token.ts` as `quickDescend`. Removed `tokenManager` from `TokenCursorBaseParams`. TokenCursor now calls `token.quickDescend` directly (3 call sites: splitAfter, insertElementAfter, insertElementBefore). EditManager no longer creates or holds a TokenManager. Package index exports `quickDescend` instead of `TokenManager`. Test file renamed `TokenManager.test.ts` → `quickDescend.test.ts`. Updated `docs/architecture.md` — replaced TokenManager section with quickDescend, updated lib/ listing to reflect current files.
