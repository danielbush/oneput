---
id: TOKEN_SPACING_MODEL__WORK
status: active
package: jsed
created: 2026-04-04
summary: Reconsider TOKEN-owned spacing because trailing spaces distort CURSOR visuals and native underline rendering
---

# refactor: reconsider token spacing model

## Problem

Jsed TOKEN's currently carry their own trailing space by default. That simplifies some TOKEN operations, but it creates rendering problems:

- CURSOR background and marker styling includes the carried trailing space unless we compensate visually
- Native underline on `<u>` extends through the trailing space, so underlined TOKEN's look too long
- The same carried-space model makes CURSOR_STATE markers harder to place cleanly, especially for APPEND and INSERT_AFTER

The rendering issues are no longer isolated to CURSOR cosmetics. Underline behaviour suggests the current spacing model may be structurally at odds with how inline text should render.

## Current understanding

We explored three approaches in `/cursor-lab`:

1. **Pure CSS compensation** — shift the CURSOR fill left and trim it on the right with pseudo-elements. This improves the CURSOR background visually but does not fix underline semantics.
2. **Pseudo-element trailing space** — move the visible trailing space to `::after` and try `text-decoration: none`. This did not stop native underline from visually extending through the generated space.
3. **Spaces between TOKEN's** — stop storing trailing space inside TOKEN text nodes and represent spacing between adjacent TOKEN's instead. This appears to align better with both CURSOR rendering and native text decoration, but it changes the meaning of COLLAPSED_TOKEN / PADDED_TOKEN and would move spacing logic into sibling-boundary management.

The main design question is now:

- Should spacing remain a TOKEN property?
- Or should spacing become a relationship between adjacent LINE_SIBLING's?

## Candidate direction: spaces between TOKEN's

In this model:

- A TOKEN contains only visible word text
- Normal word separation is represented by text nodes between TOKEN elements
- COLLAPSED_TOKEN means "no separator after this TOKEN"
- PADDED_TOKEN becomes an explicit separator before the TOKEN (or a class expressing that intent)

This would likely fix:

- CURSOR background hugging the visible word
- Underline trailing too far through carried space
- Cleaner APPEND / INSERT_AFTER marker placement

But it would also require:

- Tokenization to emit TOKEN / separator / TOKEN sequences
- JOIN / SPLIT / INSERT operations to manage separators explicitly
- Reinterpreting COLLAPSED_TOKEN and PADDED_TOKEN as boundary semantics instead of text-content semantics

## Current implementation state

The core spacing-model migration is now largely complete.

Completed:

- TOKEN text now contains visible text only
- tokenization emits alternating TOKEN and whitespace text nodes
- `getValue` / `replaceText` operate on visible TOKEN text only
- separator helpers exist in `lib/token.ts`
- `collapse` / `uncollapse` and `pad` / `unpad` now manage separator boundaries rather than literal spaces inside TOKEN text
- `remove` / `joinNext` / `joinPrevious` were updated for boundary spacing
- `TokenCursor` and the related tests were updated to work with the new spacing model
- the old `InputManager` path was superseded by `EditManager`
- `EditManager.handleInputChange(...)` was refactored into a more explicit intent-driven flow
- `Controller.createNull().input` now supports input-driving helpers for step-by-step scenario tests
- jsed docs were updated to describe boundary spacing rather than TOKEN-owned spacing

What remains is not the basic migration. The remaining work is mostly exploratory and vocabulary cleanup:

- refine spacing behaviour in exploratory document scenarios
- decide the future of `COLLAPSED_TOKEN` / `PADDED_TOKEN` vocabulary
- refine boundary editing UX, especially where space nodes and ANCHORs can coexist
- confirm any remaining edge cases in `test_doc.html`

## Changes

- **2026-04-04** — built `/cursor-lab` route in `apps/jsed-demo` to compare CURSOR rendering across serif/sans and size variants
- **2026-04-04** — confirmed that pseudo-element trailing space does not reliably stop native underline from visually extending through the space
- **2026-04-04** — identified "spaces between TOKEN's" as the main alternative to the current TOKEN-owned spacing model
- **2026-04-04** — decided the primary implementation order should be `lib/token.ts` first, then upper-layer updates in `TokenCursor.ts` and `InputManager.ts`, then exploratory validation against `test_doc.html`
- **2026-04-05** — identified that DOM-backed nullable INPUT changes selection semantics meaningfully: `InputManager` currently reads `getRange()` after `setInputValue(...)`, so some scenario tests need to move up to an EditDocument / Controller-level typing harness that preserves step-by-step INPUT transitions via `ctl.input.*`
- **2026-04-05** — changed `lib/token.ts` so TOKEN text no longer carries spacing, tokenization preserves whitespace as separate text nodes, and spacing operations became boundary-based
- **2026-04-05** — updated `TokenCursor` tests and lib tests to reflect the boundary-spacing model
- **2026-04-06** — added a DOM-backed nullable / Controller-level input-driving path so scenario tests can move the caret, type text, and emit the same input events production code uses
- **2026-04-06** — folded `InputManager` behaviour into `EditManager`, then refactored `EditManager.handleInputChange(...)` toward a logic-sandwich shape with explicit input intents
- **2026-04-06** — updated jsed architecture and vocabulary docs so they describe boundary spacing rather than TOKEN-owned trailing / leading spaces
- **2026-04-09** — added boundary-anchor exploration: insert ANCHOR before / after tag boundaries, allow ANCHOR insertion alongside existing whitespace, and support inserting an ANCHOR into an empty LINE
- **2026-04-10** — tightened empty-LINE anchor checks so IGNORABLE content does not block insertion, and extracted that rule into `lib/token.ts`

## Tasks

- [x] Treat the core migration as done and use the current code/tests as the baseline for exploratory spacing work rather than continuing to treat the refactor itself as unfinished
- [x] Introduce separator-boundary helpers in `lib/token.ts`
- [x] Rewrite tokenization and TOKEN spacing operations in `lib/token.ts` for the spaces-between-TOKEN's model
- [x] Update `TokenCursor.ts` to operate against boundary spacing rather than TOKEN-owned spaces
- [x] Replace the old `InputManager`-specific plan with `EditManager` / Controller-level input handling and tests
- [x] Add a Controller / InputController nullable typing harness backed by a real happy-dom input element so `Controller.createNull().input` can drive step-by-step INPUT / CURSOR scenario tests
- [x] Update jsed docs so the spacing model is described in boundary terms

bug

- [ ] test blank TOKEN replaceText bug

spaces - adding spaces via menu;

- [ ] if our FOCUS is on em or strong, insert space between em and strong from either side
  - `<em>...</em><strong>...</strong>` - should insert space
  - `<em>...</em>foo<strong>...</strong>` - should insert leading OR trailing space
  - `<em>...</em> <strong>...</strong>` - should NOT insert space because there is one already
  - should handle IGNORABLE Junk between em-tag and strong-tag
- [ ] if our CURSOR is on "foo" and we want a leading or trailing space:
  - `<em>...</em>|foo<strong>...</strong>` - should insert leading OR trailing space

pad / collapse review

- [ ] Decide whether `COLLAPSED_TOKEN` and `PADDED_TOKEN` remain as transitional classes / terms or should be renamed around boundary semantics
- [ ] remove collapse/uncollapse/pad/unpad token operations?

## Questions

- Are there still any real scenarios where carried-space-in-TOKEN text would have been preferable, or has the boundary model clearly won?
- What is the best vocabulary for spacing now that `COLLAPSED_TOKEN` / `PADDED_TOKEN` are legacy names over boundary behaviour?
- When a whitespace boundary already exists between tags, should inserting an ANCHOR be side-sensitive (based on current FOCUS) or normalized to one canonical DOM shape?
- Should inter-LINE anchor insertion be handled via IMPLICIT_LINE-like structure rather than the tag-boundary anchor actions?

## Initial Proposal

- Stop treating trailing space as part of TOKEN text content
- Treat spacing as a boundary between adjacent LINE_SIBLING's
- Use the current lab findings as justification for a focused design pass before implementation
