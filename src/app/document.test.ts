import { start } from './document';

test('HI_SIB - highlights siblings on focus', () => {
  start(document.createElement('DIV'), []);
  // assert event handler is set up
  // test handler calls action
});

test.todo('HI_SIB - highlights siblings on click');
