---
id: STALE_CURSOR_LINE__WORK
status: done
package: jsed
created: 2026-03-26
summary: Click-to-focus across LINE boundaries leaves CURSOR_LINE stale, causing moveNext to fire with wrong ceiling
outcome: Chose approach (2) — EditManager.handleFocusRequest detects cross-LINE clicks, calls nav.FOCUS on the target, then signals exit via onExit callback. EditDocument wires onExit to ctl.app.exit(), returning to ViewDocument with FOCUS already on the new element. Added getLine() to ITokenCursor interface.
---

# fix: Stale CURSOR_LINE on click-to-focus

Extracted from CURSOR_WALKS_NON_TOKENS__WORK.

## Problem

When editing a TOKEN and then clicking a TOKEN in a different LINE, `moveNext` fires with the old CURSOR_LINE as ceiling. This produces the warning: `findNextNode: start [HTMLSpanElement] is not contained in ceiling [HTMLDivElement]`.

The CURSOR_LINE stored on TokenCursorBase becomes stale when REQUEST_FOCUS hops across LINE boundaries.

## Possible approaches

1. Update CURSOR_LINE when the CURSOR hops to a TOKEN in a different LINE
2. Tear down the old edit session before the click triggers `moveNext`, then re-establish the LINE ceiling on REQUEST_FOCUS

## Tasks

- [x] Reproduce and characterise the exact sequence of events
- [x] Decide between approach (a) or (b) — chose (2): exit edit mode on cross-LINE click
- [x] Implement fix
- [x] Test

## Changes

- **2026-03-27** — implemented approach (2). EditManager.handleFocusRequest compares target LINE against CURSOR_LINE; on mismatch calls nav.FOCUS then onExit callback. Added onExit param to EditManager.create. EditDocument passes ctl.app.exit(). Added getLine() to ITokenCursor interface.
