---
id: CLICK_TOUCH_EDIT__WORK
status: done
package: jsed
created: 2026-03-28
summary: Click or touch on a LINE tokenizes it and enters edit mode with CURSOR on first TOKEN
related: FOCUS_TWICE_TO_EDIT__DISCUSSION
outcome: "Focus twice to edit" model implemented via immutable focus controller on Nav. Nav refactored so each AppObject owns its own instance with connect/disconnect/destroy lifecycle. EditManager creates its own Nav via edit(initialFocus) and exposes nav and cursor as public fields. TokenCursor close renamed to destroy.
---

# feat: click/touch tokenizes LINE and enters edit mode

## Problem

Click or touch on a LINE should tokenize it and put the CURSOR at the beginning of the first TOKEN. Currently clicking a LINE focuses it but doesn't enter edit mode — the user has to explicitly trigger edit mode separately.

## Design

"Focus twice to edit" model: the first click/touch on a FOCUSABLE sets FOCUS (view mode — lets the user click around the document structure without entering edit mode). A second click/touch on the *same* FOCUSABLE triggers quick-descend to find the first TOKEN and enter edit mode. This keeps navigation and editing clearly separated.

## Implementation

ViewDocument sets an immutable focus controller on its Nav that detects when REQUEST_FOCUS targets the already-focused element. On match, it launches EditDocument with the current focus element.

As part of this work, Nav was refactored so each AppObject owns its own Nav instance with an immutable focus controller passed at construction. Nav has `connect()` / `disconnect()` / `destroy()` for lifecycle management. EditManager now creates its own Nav internally via `edit(initialFocus)`.

## Tasks

- [x] Detect when REQUEST_FOCUS targets the already-focused element
- [x] On second focus, trigger quick-descend and enter edit mode
- [x] Handle edge case: LINE with no editable content (quick-descend returns null)
- [x] Test with various LINE types (plain `<p>`, nested `<div>`, LINE containing ISLAND's)
- [ ] Exploratory testing in jsed-demo
