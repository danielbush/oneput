## Conversion script

The conversion script was written to convert the maintainers initial markdown format used with a prototype of the 2br system (called project "fold"") into html.

- inadvertent katex
  - example
    - `... a$::FOO ... a$::BAR` - 2 `$` may trigger katex
  - solution
    - manual fix (script should list the issues and user should fix)
