---
id: REMOVE_TOKEN_RE_EXPORTS__WORK
status: active
package: jsed
created: 2026-03-24
summary: Remove re-exports from packages/jsed/src/lib/token.ts — update packages/jsed/src/index.ts and apps/jsed-demo/ as needed
---

## Proposal

`packages/jsed/src/lib/token.ts` re-exports traversal predicates that were historically part of that module. There's no point — remove them and have consumers import directly from `traversal.ts` (internal) or via `packages/jsed/src/index.ts` (external).

### Steps

1. Remove the re-export block in `packages/jsed/src/lib/token.ts` (the `export { isFocusable, isIgnorable, ... } from './traversal.js'` section)
2. Update `packages/jsed/src/index.ts` to export the needed symbols directly from `traversal.ts`
3. Update all internal imports (`import * as token from './token.js'` or similar) that rely on re-exported symbols — switch them to import from `traversal.ts` directly
4. Update `apps/jsed-demo/` if it imports any of the affected symbols
5. `task check` and `task test` pass
