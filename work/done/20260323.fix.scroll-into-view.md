---
id: SCROLL_INTO_VIEW__WORK
status: done
package: jsed
created: 2026-03-23
summary: Scroll container doesn't follow FOCUS or CURSOR when they move beyond the visible area
outcome: Added `el.scrollIntoView?.({ block: 'nearest', inline: 'nearest' })` in Nav.#updateFocus (covers all FOCUS moves) and TokenCursorBase.setToken (covers all CURSOR moves). Optional chaining handles jsdom in tests. All 150 tests pass.
---

# fix: Scroll container when FOCUS or CURSOR moves beyond visible area

## Problem

When navigating with FOCUS (Nav) or CURSOR (TokenCursor) inside a scrollable container (e.g. `overflow-y: scroll`), the container doesn't scroll to keep the focused/cursored element visible. The element moves off-screen and the user loses track of where they are.

Reproducible using the "Scrollable container" example in `apps/jsed-demo/static/test_doc.html`.

## Goal

When FOCUS or CURSOR moves to an element that is outside the visible area of its scroll container, scroll the container to bring the element into view.

## Changes

- **2026-03-24** — Added `el.scrollIntoView?.({ block: 'nearest', inline: 'nearest' })` in `Nav.#updateFocus` (line 126) and `TokenCursorBase.setToken` (line 81). Optional chaining used so jsdom in tests silently no-ops. `block: 'nearest'` avoids jarring jumps and is a no-op when already visible. All 150 tests pass.

## Scope

- Only touched `packages/jsed/src/Nav.ts` and `packages/jsed/src/TokenCursorBase.ts`
