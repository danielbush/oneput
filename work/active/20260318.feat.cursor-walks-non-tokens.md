---
id: CURSOR_WALKS_NON_TOKENS__WORK
status: active
package: jsed
created: 2026-03-18
summary: Expand CURSOR traversal beyond TOKEN's so LINE_SIBLING's include CURSOR_OPAQUE elements (ISLAND, CURSOR_BOUNDARY) and CURSOR_TRANSPARENT elements (INLINE, BLOCK_TRANSPARENT). Scenarios (1) and (2) done; (3) CURSOR_BOUNDARY deferred
---

## Proposal

The CURSOR currently visits only TOKEN's (and recurses through INLINE's). We expand it so that LINE_SIBLING's include three categories of non-TOKEN FOCUSABLE's:

| Cat | visit | descend | Detection | Examples |
|-----|-------|---------|-----------|----------|
| ✅ (1) ISLAND | CURSOR=yes, FOCUS=yes | CURSOR=no, FOCUS=no | Implicit (e.g. `.katex`, `<img>`) or explicit class | Katex equations, images — opaque elements the CURSOR hops over |
| (2) BLOCK_TRANSPARENT | CURSOR=no, FOCUS=yes | CURSOR=yes, FOCUS=yes | Default for any non-INLINE, non-ISLAND FOCUSABLE | Nested `<div>`, `<section>`, inline-block `<span>` — CURSOR descends into their TOKEN's seamlessly |
| (3) CURSOR_BOUNDARY | CURSOR=yes, FOCUS=yes | CURSOR=no, FOCUS=yes | Explicit `jsed-cursor-opaque` class | Elements FOCUS can descend but CURSOR treats as opaque |

Category (4) (CURSOR visit=no, descend=no) is deferred — no concrete use case yet.

### Taxonomy summary

Non-TOKEN FOCUSABLE's group into two CURSOR behaviors:

- **CURSOR_TRANSPARENT** (descend=yes, visit=no) — the CURSOR passes through to visit TOKEN children
  - **INLINE** — inline-level markup (`<em>`, `<a>`)
  - **BLOCK_TRANSPARENT** — everything else: block, inline-block, etc. (nested `<div>`, `<section>`)
- **CURSOR_OPAQUE** (descend=no, visit=yes) — the CURSOR lands on the element itself
  - **ISLAND** — externally managed content (katex, `<img>`)
  - **CURSOR_BOUNDARY** — explicitly marked elements FOCUS can descend but CURSOR cannot

### Key design decisions

**LINE as explicit parameter.** `TokenCursorBase.setToken` calculates the LINE when assigned a TOKEN. `getNextLineSibling(el, line)` and `getPreviousLineSibling(el, line)` take the LINE as an explicit ceiling — they never call `getLine` internally. This solves the deep nesting problem: the LINE is determined once at entry time, not inferred per-hop.

**`getLine` stays as-is** for FOCUS-level code (Nav). It just stops being used by CURSOR-level code.

**visit/descend predicates expand:**
- `visit`: `isToken || isIsland || isCursorBoundary` — things the CURSOR lands on
- `descend`: `isInline || isBlockTransparent` — things the CURSOR passes through transparently (visiting their TOKEN children)

**Naming.** NESTED_LINE remains a structural term (a LINE inside another LINE). The behavioral annotation for (3) is `CURSOR_BOUNDARY` / `jsed-cursor-opaque`.

### Tests

126 passing, 6 failing. The 6 failures are all (3) CURSOR_BOUNDARY tests — expected, since that scenario is deferred.

## ~~Plan for (2) BLOCK_TRANSPARENT~~ — DONE

Implemented. Key decisions that landed:

- **LINE as explicit parameter** — `getNextLineSibling(el, line)` / `getPreviousLineSibling(el, line)` take the LINE as a ceiling, determined once at entry time. `getLine` stays for FOCUS-level code (Nav); CURSOR-level code uses the stored LINE.
- **`isBlockTransparent`** — `isLine(el) && !isCursorBoundary(el)`, added to traversal.ts alongside `isInline`, `isLine`, `isIsland`. `isCursorBoundary` is a stub returning `false` (hook point for scenario 3).
- **Lazy tokenization on cursor move** — when `moveNext`/`movePrevious` walks into an untokenized BLOCK_TRANSPARENT, it tokenizes just that one level before descending. Preserves SHALLOW_TOKENIZATION.
- **TokenCursorBase stores `line`** — set when the CURSOR is first positioned, passed to all LINE_SIBLING traversal calls.

## Changes

- **2026-03-18** — created ticket, wrote 26 failing tests for scenarios (1), (2), (3)
- **2026-03-18** — scenario (1) ISLAND done: CURSOR visits ISLANDs as opaque LINE_SIBLINGs, editing ops guarded with `isOnToken()`
- **prior session** — scenario (2) BLOCK_TRANSPARENT done: LINE as explicit parameter, lazy tokenization on cursor move, `isBlockTransparent` predicate. All (2) tests passing
- **2026-03-22** — updated ticket to reflect (1) and (2) complete. 126 passing, 6 failing (all scenario 3 CURSOR_BOUNDARY — deferred)

## Known issues (found in exploratory testing)

- **Space loss around ISLAND** — when `tokenizeLine` processes text adjacent to an ISLAND (e.g. `<p>aaa <span class="katex">x²</span> bbb</p>`), the leading space in `' bbb'` is stripped by `split(/\s+/).filter(Boolean)`. Normally the previous TOKEN's trailing space provides the visual gap, but the ISLAND has no trailing space — so the ISLAND and `bbb` render flush.
  - **Proposed approach: PADDED_TOKEN.** The TOKEN after the ISLAND owns the gap via a leading space. This introduces PADDED_TOKEN as the counterpart to COLLAPSED_TOKEN. A TOKEN has 2² = 4 spacing states:
    - unpadded + uncollapsed: `'foo '` — the default
    - unpadded + collapsed: `'foo'` — COLLAPSED_TOKEN (existing)
    - padded + uncollapsed: `' foo '` — PADDED_TOKEN
    - padded + collapsed: `' foo'` — PADDED_TOKEN + COLLAPSED_TOKEN
    PADDED_TOKEN adds a leading space; COLLAPSED_TOKEN removes the trailing space. Both are per-TOKEN, both are togglable by the user.
    - PADDED is needed when the previous LINE_SIBLING doesn't carry its own trailing space (e.g. an ISLAND).
    - In NEGATIVE_SPACE, a stale leading space (e.g. if the ISLAND is later removed) is visually harmless — the browser collapses it. A periodic or hook-based cleanup could normalize it.
    - In POSITIVE_SPACE, the leading space is visible and correct — the original whitespace should be preserved.
    - User toggling: "toggle pad" key binding when previous LINE_SIBLING is an ISLAND, mirroring "toggle collapse" for TOKEN-to-TOKEN gaps.
    - Implementation: `createToken`/`replaceText` currently normalize to `text + ' '`. They'd need to preserve a leading space if present. `getValue` currently trims — needs to be aware of leading space. Track via a CSS class (like `JSED_TOKEN_COLLAPSED` for trailing → e.g. `JSED_TOKEN_PADDED` for leading).
    - Wrapping the ISLAND was considered and rejected — ISLAND internals are externally managed (e.g. katex renderer), wrapping changes `parentNode` which could break re-renders/event handlers, and wrapping block-level elements (scenarios 2/3) in inline spans is invalid HTML.
- **Typing over ISLAND** — when the CURSOR lands on an ISLAND, `onTokenChange` fires with the ISLAND element. The input pipeline puts its text content (`x²`) into the input field, and the user can type over it — hitting `replace`/`replaceText` and corrupting the ISLAND's DOM. The `onTokenChange` callback (or the input layer above it) needs to distinguish TOKEN from non-TOKEN LINE_SIBLING's so it can make the input read-only or disable it while the CURSOR is on an ISLAND.

### Housekeeping

After the feature lands:

- ~~**Update vocabulary.md**~~ — ✅ all terms current. ISLAND, LINE_SIBLING, PADDED_TOKEN, TOGGLE_PADDED, BLOCK_TRANSPARENT, CURSOR_TRANSPARENT, CURSOR_OPAQUE, CURSOR_LINE added. CURSOR_BOUNDARY remains marked as planned (scenario 3)
- **Type checks for tests** — look into enabling type checking for test files (currently only production code is checked)
- **`isToken` type guard** — review whether to remove the type guard on `isToken` now that `setToken` and LINE_SIBLING traversal may handle non-TOKEN elements
- **Revisit NESTED_LINE naming** — now that BLOCK_TRANSPARENT exists, assess whether NESTED_LINE is still a useful distinct term or if it's just "a LINE that happens to be inside another LINE"
- **Old ISLAND test** — the "CURSOR visits the island" test in `getNextLineSibling / getPreviousLineSibling` duplicates scenario (1) coverage; integrate it into the `(1) ISLAND` describe block or remove it
- ~~**Guard TOKEN-only editing ops**~~ — ✅ added `isOnToken()` guard to all TokenCursor editing methods; EditManager guards `handleInputChange`, `handleSelectionChange`, and `handleTokenChange` to clear/no-op on non-TOKEN LINE_SIBLING's
- **Toggle token spacing** — unify TOGGLE_COLLAPSE and TOGGLE_PADDED into a single "toggle token spacing" concept. TOGGLE_COLLAPSE controls trailing space (next-side); TOGGLE_PADDED controls leading space (previous-side). The binding should intelligently offer TOGGLE_PADDED when the previous LINE_SIBLING is an ISLAND (or other non-TOKEN that doesn't provide trailing space). Could be one key binding that inspects context, or two distinct bindings
- **Visible vs invisible IGNORABLE's** — `getPreviousVisibleSibling`/`getNextVisibleSibling` skip all IGNORABLE's, but some IGNORABLE's may be visually present (e.g. decorative markers) while others are truly invisible (e.g. undo bookmarks). This distinction could affect PADDED_TOKEN toggling and spacing decisions — a visible IGNORABLE between an ISLAND and a TOKEN might mean the user expects a gap, while an invisible one shouldn't influence spacing. Review whether IGNORABLE needs subclasses or whether the current blanket skip is sufficient
- **TokenCursor editing tests with PADDED/COLLAPSED** — add TokenCursor tests for `replace`, `delete`, `append`, `joinNext`, `joinPrevious`, `splitBefore`, `splitAfter` that verify correct behaviour with PADDED_TOKEN's and COLLAPSED_TOKEN's (and the combination). Ensure `replaceText` preserves padding/collapse state, `joinNext`/`joinPrevious` handle spacing correctly when one operand is padded, and `delete` of a PADDED_TOKEN transfers or drops the padding appropriately
- **DOM type unification** — review the mix of `ChildNode`, `ParentNode`, `HTMLElement`, `Element`, `Node`, and `EventTarget` across function signatures in jsed. The current variety causes friction (frequent casts, union types that don't narrow well). Likely warrants a separate refactor spec to settle on a consistent approach — e.g. `HTMLElement` as the default for jsed's own functions, with narrower DOM types only at boundaries that genuinely need them
- **Remove `tagImplicitLines`** — already disabled (commented out) in `token.ts`. With BLOCK_TRANSPARENT now handling nested block elements seamlessly, IMPLICIT_LINE's are likely unnecessary. Delete `tagImplicitLines`, `buildImplicitLine`, `JSED_IMPLICIT_CLASS`, and the `isImplicitLine` predicate. Check for any remaining references before removing

---

## Initial idea

The CURSOR currently visits TOKEN's and recurses into INLINE's.
We want to expand this behaviour so that LINE_SIBLING's include other elements besides TOKEN's.

We need to review the taxonomy.

By definition FOCUSABLE means visit=yes for FOCUS .

- (1) non-INLINE FOCUSABLE where...
  - FOCUS visit=yes, descend=no
  - CURSOR visit=yes, descend=no
  - COMMENT: let's assume these are ISLAND's as we defined before except the CURSOR can now visit; this would be something that naturally sits on the line alongside tokens but should be externally managed such as katex equations that form part of the sentence or even in latex block mode; this is the classic ISLAND (TODO: verify definition fits pending the change to CURSOR now visiting it)
- (2) non-INLINE FOCUSABLE where...
  - FOCUS visit=yes, descend=yes
  - CURSOR visit=yes, descend=yes
  - COMMENT: I think ISLAND's (1) are special, and we should distinguish them by a class or they have obvious signatures such as katex; this category might be a default; that is it's not INLINE, it's not an ISLAND, so the default behaviour is to let the CURSOR descend into it like an INLINE
- (3) non-INLINE FOCUSABLE where...
  - FOCUS visit=yes, descend=yes
  - CURSOR visit=yes, descend=no
  - COMMENT: because we have (2), we want to allow the case where for some reason we don't want the CURSOR to descend; because FOCUS can descend them, I tend to think of these as NESTED_LINE's; I don't think these should be common, so these might have some kind of class in the DOM to identify them;  so a user would have to mark them as being non-descendable by the CURSOR;
  - TODO: I'm not sure I like NESTED_LINE - happy to rethink the name, and also adjust the vocab definitions accordingly
- (4) non-INLINE FOCUSABLE where...
  - FOCUS visit=yes, descend=yes
  - CURSOR visit=no, descend=no
  - COMMENT: finally because we have (3) it opens the possibility for (4) where we have something that is part of the document content but we just don't want the cursor to visit it at all; so it is like an IGNORABLE but only for the CURSOR; possibly these could be markers that are part of the document but are never edited via the cursor; but I'm struggling for a use case
- (5) TODO: do other combinations of FOCUSABLE's make any sense?

Key decision for discussion: how to distinguish (1) - (4)?  As mentioned int he COMMENT's above, I think ISLAND's (1) are special and are flagged in some way including an epxlicity class that we can add; (2) is a default, so this means if the user doesn't do anything, the CURSOR will descned them and visit all their TOKEN's just like an INLINE; and (3) and (4) are special cases that we have to explicitly flag with a CSS class.

The next thing to be wary of is getLine and how the current LINE is established especially during repeated calls to getNextLineSibling/getPreviousLineSibling as used in moveNext, movePrevious of TokenCursor (the CURSOR).  The CURSOR should never explicitly be set on (4) and should probably refuse if asked (setToken); when finding next or previous LINE_SIBLING's it should obviously not visit (4) - getLine should never treat it as a LINE. If T is (3), the system knows it's a "cursor ISLAND" because it's explicitly marked, so it can never be a LINE and getLine should be adjusted accordingly.  If T is (2), it treats it like INLINE, but the problem here is that it looks like and could be a LINE in its own right.  Perhaps getParentLine is the solution for all cases?  It gets the parent element, and the calls getLine on it.  That would mean getNextLineSibling / getPreviousLineSibling always look to the parent to establish the LINE.  TODO: is this flawed, what could go wrong?

Before we code anything up, we can write the failing tests to highlight the above situations.  Even though (2) is similar to INLINE, I'd like to still distinguish INLINE's from (2) in the event we decide to revert the changes we're proposing here.

So we should extend moveNext/movePrevious tests for TokenCursor with failing test cases for (1), (2), (3), (4) and pretend like we've already solved some of the issues above.  Then we can make a final decision.

The tests should include implicit ISLAND's liek katex ISLAND's as well as proposed explicit ones.  Include ISLAND's at the very beginning of the LINE, the very end of the LINE and in the middle.  And we want to test it within INLINE's.  Test visit and descend behave accordingly.  Repeat for (2),(3),(4).
