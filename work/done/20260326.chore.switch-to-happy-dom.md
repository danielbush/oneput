---
id: SWITCH_TO_HAPPY_DOM__WORK
status: done
package: jsed
created: 2026-03-26
summary: Switch jsed test environment from jsdom to happy-dom — fixes 65 scrollIntoView failures
outcome: Replaced jsdom with happy-dom in vite.config.ts and package.json. All 166 tests now pass (previously 65 failed due to jsdom lacking scrollIntoView support).
---

# chore: Switch jsed test environment to happy-dom

jsdom does not implement `scrollIntoView`, causing 65 test failures in Nav, TokenCursor, and JsedDocument test suites. happy-dom supports it natively and is already used by `oneput` and `jsed-demo`.

## Changes

- `packages/jsed/vite.config.ts` — `environment: 'jsdom'` → `environment: 'happy-dom'`
- `packages/jsed/package.json` — removed `jsdom` and `@types/jsdom`, added `happy-dom`

## Verification

- `task jsed:test` — 166 passed, 1 todo (was 101 passed, 65 failed)
- `task jsed:check` — passes
