# https://taskfile.dev

version: "3"
silent: false
set:
  - errexit
  - nounset
  - pipefail
# dotenv: [".env"]

includes:
  oneput:
    taskfile: ./packages/oneput/Taskfile.yml
    dir: ./packages/oneput

tasks:
  default:
    cmds:
      - task --list-all

  install:
    cmds:
      - pnpm i
